# <Project name> — backlog

**Last updated:** YYYY-MM-DD (the agent maintains this file; the user only talks)
**Authority:** ssot for this project's tasks and their status

## Statuses

`TODO` queued · `IN PROGRESS` active · `BLOCKED` waiting on something named · `DONE` finished
and verified. A `DONE` nobody verified is `IN PROGRESS` — the delivery-reality lens will find it.

## Active

### T-001: <one line, imperative>
**Status:** TODO · **Priority:** P0 | P1 | P2 · **Opened:** YYYY-MM-DD
**Spec:** `plans/YYYY-MM-DD-<slug>.md` (or "none — too small for a spec")

What and why, 1-3 sentences. Not how — how lives in the spec.

**Acceptance:**
- [ ] checkable statement, not "works"
- [ ] the gate that proves it (command / walked path / query)

**Blocked by:** `<the named thing>` — only on `BLOCKED`, and it names who unblocks it.

---

## Done

> Moved here on completion, `[YYYY-MM-DD]`-prefixed, newest first. Keep one line each — the
> detail is already in the handoff of the session that closed it.

- [YYYY-MM-DD] T-000 — <one line> · verified by `<gate>`

## Dropped

> Dropped ≠ done. One line + why, so the same idea doesn't get re-opened in three weeks.
